---
import Base from "../layouts/Base.astro";
import { site } from "../content/site";

const title = `Work — ${site.name}`;
---

<Base
  title={title}
  description={`Work from ${site.name}`}
  canonicalPath="/work"
>
  <article class="prose">
    <h1>Work</h1>

    <p class="lead">
      Our portfolio showcases the projects we've completed and the results we've achieved.
    </p>

    <p>
      This is where you can display your work, case studies, and project highlights.
    </p>
  </article>
</Base>
